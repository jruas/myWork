#This program takes an input number
#Returns the abs of the number multiplied by 100
#Meaning that receives dollars and outputs in cents I take an input of an amount

a=float(input('Please insert a number: '))
b=abs(a)
c=b*100

print('The amount is {}'.format(c))